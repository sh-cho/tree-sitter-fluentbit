================================================================================
directives
================================================================================
@INCLUDE abc.conf
@SET my_env=1

--------------------------------------------------------------------------------
(config
  (directive
    (directive_include
      (value_type)))
  (directive
    (directive_set
      (identifier)
      (value_type))))



================================================================================
directives_set with space
================================================================================

@SET a      = 1

--------------------------------------------------------------------------------
(config
  (directive
    (directive_set
      (identifier)
      (value_type))))



================================================================================
directives_set without space
================================================================================
@SET a=1

--------------------------------------------------------------------------------
(config
  (directive
    (directive_set
      (identifier)
      (value_type))))


================================================================================
section
================================================================================
[INPUT]
    KEY1  VALUE1
    KEY2  VALUE2

--------------------------------------------------------------------------------
(config
  (section
    (section_header
      (section_header_type))
    (section_body
      (entry
        (identifier)
        (value_type))
      (entry
        (identifier)
        (value_type)))))
