================================================================================
directives
================================================================================
@INCLUDE abc.conf
@SET my_env=1

--------------------------------------------------------------------------------
(config
  (directive
    (directive_include
      (value_type)))
  (directive
    (directive_set
      (identifier)
      (value_type))))
